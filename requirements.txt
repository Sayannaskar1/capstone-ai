# ── Core app ───────────────────────────────────────────────────────────────────
streamlit
langgraph
langchain-community
langchain-core
langchain-groq
python-dotenv
pymupdf

# ── Dependencies removed to prevent Streamlit Cloud 1GB memory crashes ───────
# (RAG is completely bypassed, so these heavy ML libraries are no longer needed)

# ── PDF report export ──────────────────────────────────────────────────────────
reportlab>=4.1.0

# ── Charts & visualisation ─────────────────────────────────────────────────────
plotly>=5.18.0
